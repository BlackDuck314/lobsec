---
phase: 07.1-ninja-scraper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/scraper/pyproject.toml
  - packages/scraper/src/ninja_scraper/__init__.py
  - packages/scraper/src/ninja_scraper/engine/__init__.py
  - packages/scraper/src/ninja_scraper/engine/crawler.py
  - packages/scraper/src/ninja_scraper/engine/mission.py
  - packages/scraper/src/ninja_scraper/engine/storage.py
  - packages/scraper/src/ninja_scraper/stealth/__init__.py
  - packages/scraper/src/ninja_scraper/stealth/browser.py
  - packages/scraper/src/ninja_scraper/utils/__init__.py
  - packages/scraper/src/ninja_scraper/utils/retry.py
  - packages/scraper/src/ninja_scraper/utils/logging.py
  - packages/scraper/src/ninja_scraper/api/__init__.py
  - packages/scraper/src/ninja_scraper/api/main.py
  - packages/scraper/src/ninja_scraper/api/schemas.py
  - packages/scraper/src/ninja_scraper/api/auth.py
autonomous: true
requirements:
  - COLL-01
  - COLL-02
  - COLL-03
  - COLL-04
  - COLL-05
  - COLL-15

must_haves:
  truths:
    - "Ninja Scraper package exists at packages/scraper/ with valid pyproject.toml"
    - "FastAPI app starts and responds to /health on port 18791"
    - "Bearer token authentication rejects unauthenticated requests with 401"
    - "Mission YAML files can be loaded and validated by Pydantic model"
    - "Patchright can launch a stealth Chromium browser and navigate to a page"
    - "HTTP downloads work via httpx for CSV/API sources"
    - "Crawlee storage writes output to /opt/lobsec/data/raw/ (not default ./storage/)"
  artifacts:
    - path: "packages/scraper/pyproject.toml"
      provides: "Package metadata and dependencies"
      contains: "crawlee"
    - path: "packages/scraper/src/ninja_scraper/api/main.py"
      provides: "FastAPI application with /scrape, /crawl, /missions, /health endpoints"
      exports: ["app"]
    - path: "packages/scraper/src/ninja_scraper/engine/mission.py"
      provides: "Mission spec loader and Pydantic validation"
      exports: ["Mission", "load_mission", "load_all_missions"]
    - path: "packages/scraper/src/ninja_scraper/engine/crawler.py"
      provides: "Crawlee PlaywrightCrawler wrapper with mission execution"
      exports: ["run_http_mission", "run_browser_mission"]
    - path: "packages/scraper/src/ninja_scraper/stealth/browser.py"
      provides: "Patchright browser launcher with stealth config"
      exports: ["get_patchright_browser"]
    - path: "packages/scraper/src/ninja_scraper/api/auth.py"
      provides: "Bearer token authentication dependency"
      exports: ["verify_token"]
  key_links:
    - from: "packages/scraper/src/ninja_scraper/api/main.py"
      to: "packages/scraper/src/ninja_scraper/engine/crawler.py"
      via: "import and call run_http_mission/run_browser_mission"
      pattern: "from ninja_scraper\\.engine\\.crawler import"
    - from: "packages/scraper/src/ninja_scraper/api/main.py"
      to: "packages/scraper/src/ninja_scraper/api/auth.py"
      via: "FastAPI Depends(verify_token)"
      pattern: "Depends\\(verify_token\\)"
    - from: "packages/scraper/src/ninja_scraper/engine/crawler.py"
      to: "packages/scraper/src/ninja_scraper/stealth/browser.py"
      via: "import for browser_scrape missions"
      pattern: "from ninja_scraper\\.stealth\\.browser import"
---

<objective>
Build the complete Ninja Scraper Python package at `packages/scraper/` — the general-purpose scraping engine for the lobsec ecosystem.

Purpose: Replace the failed TypeScript collectors with a production-grade Python scraping engine that uses Crawlee for orchestration, Patchright for stealth browser automation, and FastAPI for service access. This is the foundation that all 7 UAE RE missions (Plan 02) and all future collectors (Phases 8-9, 22 more sources) will use.

Output: A fully functional `@lobsec/scraper` Python package with engine core, FastAPI API layer, Patchright stealth browser, mission spec framework, and Bearer token authentication.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-ninja-scraper/07.1-CONTEXT.md
@.planning/phases/07.1-ninja-scraper/07.1-RESEARCH.md
@.planning/uae-re-playbook.md

Key design decisions from CONTEXT.md:
- General-purpose engine (NOT UAE-RE-specific)
- Crawlee PlaywrightCrawler + Patchright for stealth
- FastAPI with /scrape, /crawl, /missions endpoints (Firecrawl-inspired)
- YAML missions in packages/scraper/missions/
- Own venv at /opt/lobsec/scraper-venv/
- Port 18791 (loopback only), Bearer token auth
- Conservative defaults: max_concurrency=2, 2-6s random delays
- Anti-bot: graceful 403/CAPTCHA detection, skip and alert (no aggressive retry)

From RESEARCH.md:
- Start with Option A for Patchright/Crawlee integration: use Patchright pages directly, bypass Crawlee's browser pool
- BackgroundTasks sufficient for <10min scrapes
- Set CRAWLEE_STORAGE_DIR env var for custom output directory
- Add schema_version to mission YAML for future migration
- Timeout hierarchy: playwright < crawlee < mission < fastapi
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scraper package with engine core (Crawlee, Patchright, missions, storage)</name>
  <files>
    packages/scraper/pyproject.toml
    packages/scraper/src/ninja_scraper/__init__.py
    packages/scraper/src/ninja_scraper/engine/__init__.py
    packages/scraper/src/ninja_scraper/engine/crawler.py
    packages/scraper/src/ninja_scraper/engine/mission.py
    packages/scraper/src/ninja_scraper/engine/storage.py
    packages/scraper/src/ninja_scraper/stealth/__init__.py
    packages/scraper/src/ninja_scraper/stealth/browser.py
    packages/scraper/src/ninja_scraper/utils/__init__.py
    packages/scraper/src/ninja_scraper/utils/retry.py
    packages/scraper/src/ninja_scraper/utils/logging.py
  </files>
  <action>
    Create the `packages/scraper/` directory structure per RESEARCH.md recommended layout.

    **pyproject.toml:**
    - name: `lobsec-scraper`
    - Python >=3.11
    - Dependencies: crawlee[playwright], patchright, fastapi, uvicorn[standard], pyyaml, httpx, tenacity, structlog, pydantic>=2.0
    - Dev dependencies: pytest, pytest-asyncio
    - Entry point: `ninja-scraper = ninja_scraper.api.main:main`

    **engine/mission.py — Mission spec loader:**
    - Pydantic models: `RetryConfig`, `ConcurrencyConfig`, `Mission`
    - Mission.type: Literal["http_download", "browser_scrape", "api_call"]
    - Mission.frequency: Literal["daily", "weekly", "monthly", "quarterly"]
    - Fields: name, description, type, frequency, priority (1-5), source (dict), extraction (dict), output (dict), retry (RetryConfig), timeout_ms, concurrency (ConcurrencyConfig), areas (optional list), schema_version (default "1.0")
    - `load_mission(path: str) -> Mission`: Load and validate single YAML file
    - `load_all_missions(directory: str) -> dict[str, Mission]`: Load all *.yml from directory
    - Use PyYAML safe_load + Pydantic validation
    - Log validation errors with structlog

    **engine/crawler.py — Mission execution engine:**
    - `async def run_http_mission(mission: Mission, output_dir: str) -> MissionResult`: For http_download missions. Use httpx.AsyncClient to download CSV/JSON. Respect mission.retry config. Save to mission.output.path pattern (substitute {date} with today's date). Return MissionResult with success, file_path, row_count, duration_ms, error.
    - `async def run_browser_mission(mission: Mission, output_dir: str) -> MissionResult`: For browser_scrape missions. Use Patchright (via stealth/browser.py) — do NOT use Crawlee's browser pool. Create new browser context per mission. Navigate to source URL(s). If mission.areas exists, iterate over areas with random delay (mission.concurrency.delay_between_requests_ms range). Extract data using mission.extraction.selectors (use page.query_selector_all). Save extracted data as JSON. Handle 403/CAPTCHA: if mission.retry.skip_on_403, log warning and skip (do NOT retry). Enforce mission.timeout_ms with asyncio.timeout(). Return MissionResult.
    - `MissionResult` dataclass: success (bool), file_path (str|None), row_count (int), duration_ms (int), error (str|None), mission_name (str)
    - Add random delay between 2-6s for browser missions (configurable via mission.concurrency)
    - **Timeout hierarchy (CRITICAL):** Set explicit timeout layers:
      - Playwright page.goto/waitForSelector: default_timeout = 30_000ms (30s)
      - Crawlee request_handler_timeout_secs = (mission.timeout_ms / 1000) - 60 (leave 60s buffer)
      - Mission-level: asyncio.timeout(mission.timeout_ms / 1000) wraps entire execution
      - FastAPI-level: uvicorn timeout = 900s (15 min, above any mission timeout)
      - Rule: playwright < crawlee < mission < fastapi

    **engine/storage.py — Output directory management:**
    - Default base_dir: `/opt/lobsec/data/raw/`
    - `ensure_output_dir(mission_name: str) -> Path`: Create subdirectory under base_dir
    - `get_output_path(mission_name: str, extension: str) -> Path`: Generate timestamped output path like `{base_dir}/{mission_name}/2026-03-12.csv`
    - Set CRAWLEE_STORAGE_DIR environment variable to base_dir at module import

    **stealth/browser.py — Patchright integration:**
    - `async def get_patchright_browser() -> tuple[Browser, Playwright]`: Launch Patchright Chromium headless with stealth args (--disable-blink-features=AutomationControlled, --disable-dev-shm-usage, --no-sandbox)
    - `async def create_stealth_context(browser: Browser) -> BrowserContext`: Create new context with realistic viewport (1920x1080), locale (en-US), timezone (Asia/Dubai)
    - Use `patchright.async_api.async_playwright` (NOT playwright)

    **utils/retry.py — Exponential backoff with jitter:**
    - `async def retry_with_jitter(func, retry_config: RetryConfig, logger) -> Any`: Custom retry loop using RetryConfig from mission spec. Implements exponential backoff with +/-25% jitter. Respects skip_on_403 (checks httpx.HTTPStatusError status code). Logs each retry attempt with structlog.

    **utils/logging.py — Structured logging setup:**
    - `def setup_logging(level: str = "INFO") -> structlog.BoundLogger`: Configure structlog with JSON output, timestamped, async-safe. Add processor for scraper context (mission_name, attempt).
  </action>
  <verify>
    <automated>cd /root/lobsec && python3 -c "
import sys, tempfile, os
sys.path.insert(0, 'packages/scraper/src')
from ninja_scraper.engine.mission import Mission, RetryConfig, load_mission
from ninja_scraper.engine.storage import ensure_output_dir, get_output_path
from ninja_scraper.utils.logging import setup_logging
print('All engine imports OK')
# Validate Mission model with test data
m = Mission(name='test', description='test', type='http_download', frequency='weekly', priority=1, source={'url': 'https://example.com'}, extraction={'format': 'csv'}, output={'path': '/tmp/test.csv', 'format': 'csv'}, timeout_ms=30000)
print(f'Mission validation OK: {m.name}')
assert m.schema_version == '1.0', f'Expected schema_version 1.0, got {m.schema_version}'
print(f'schema_version OK: {m.schema_version}')
# Test load_mission with a temp YAML file
import yaml
test_yaml = {'name': 'test-load', 'description': 'Test YAML load', 'type': 'http_download', 'frequency': 'daily', 'priority': 1, 'source': {'url': 'https://example.com'}, 'extraction': {'format': 'csv'}, 'output': {'path': '/tmp/test.csv', 'format': 'csv'}, 'timeout_ms': 30000, 'schema_version': '1.0'}
with tempfile.NamedTemporaryFile(mode='w', suffix='.yml', delete=False) as f:
    yaml.dump(test_yaml, f)
    tmppath = f.name
loaded = load_mission(tmppath)
os.unlink(tmppath)
assert loaded.name == 'test-load', f'load_mission failed: {loaded.name}'
print(f'load_mission OK: {loaded.name}')
"</automated>
  </verify>
  <done>
    - packages/scraper/ directory exists with pyproject.toml and all engine modules
    - Mission Pydantic model validates YAML-like input correctly
    - Storage module generates proper output paths under /opt/lobsec/data/raw/
    - Retry utility implements exponential backoff with jitter
    - Patchright browser module has async launcher function
  </done>
</task>

<task type="auto">
  <name>Task 2: Build FastAPI service layer with authentication and job management</name>
  <files>
    packages/scraper/src/ninja_scraper/api/__init__.py
    packages/scraper/src/ninja_scraper/api/main.py
    packages/scraper/src/ninja_scraper/api/schemas.py
    packages/scraper/src/ninja_scraper/api/auth.py
  </files>
  <action>
    Build the FastAPI application that exposes the Ninja Scraper engine as an HTTP service.

    **api/auth.py — Bearer token authentication:**
    - Use FastAPI HTTPBearer security scheme
    - `async def verify_token(credentials: HTTPAuthorizationCredentials = Depends(security)) -> str`: Compare credentials.credentials against SCRAPER_AUTH_TOKEN env var. Return 500 if env var missing. Return 401 with WWW-Authenticate header if token invalid.

    **api/schemas.py — Pydantic request/response models:**
    - `ScrapeRequest`: url (HttpUrl), format (str, default "json"), timeout_ms (int, default 30000)
    - `ScrapeResponse`: success (bool), data (dict|None), error (str|None), duration_ms (int)
    - `CrawlRequest`: mission_name (str), params (dict, default {}), force (bool, default False)
    - `CrawlResponse`: job_id (str), status (str: queued|running|completed|failed), message (str)
    - `JobStatus`: job_id (str), status (str), mission_name (str), started_at (str|None), completed_at (str|None), result (MissionResult|None), error (str|None)
    - `MissionInfo`: name (str), description (str), type (str), frequency (str), priority (int)

    **api/main.py — FastAPI application:**
    - Create FastAPI app with title "Ninja Scraper", version "1.0.0"
    - Job tracking: in-memory dict `jobs: dict[str, JobStatus]` for background task state
    - Endpoint `POST /scrape` (sync): Execute single-page scrape immediately. Use run_http_mission or run_browser_mission based on heuristic (if URL matches known browser-needed patterns, use browser; otherwise HTTP). Protected by verify_token. Returns ScrapeResponse.
    - Endpoint `POST /crawl` (async): Queue a named mission for background execution. Generate UUID job_id. Add BackgroundTasks task to execute mission. Return CrawlResponse with job_id and status="queued". Protected by verify_token.
    - Endpoint `GET /crawl/{job_id}`: Return JobStatus for background crawl. Protected by verify_token.
    - Endpoint `GET /missions`: List all loaded missions (name, description, type, frequency, priority). Protected by verify_token.
    - Endpoint `GET /health`: Unauthenticated health check. Return {"status": "ok", "missions_loaded": N, "version": "1.0.0"}.
    - On startup: load all missions from MISSIONS_DIR env var (default: packages/scraper/missions/). Log count.
    - Background task runner: `async def execute_mission(job_id: str, mission_name: str, params: dict)`: Update job status to "running". Call run_http_mission or run_browser_mission based on mission.type. Update job status to "completed" or "failed". Catch all exceptions.
    - `def main()`: Entry point. Run uvicorn with host="127.0.0.1", port=18791.

    **Important implementation notes:**
    - All endpoints except /health require Bearer token auth via Depends(verify_token)
    - Bind to 127.0.0.1 only (loopback) — consistent with gateway/proxy pattern
    - Use structlog for all request logging
    - Set maximum 10 concurrent background tasks (warn if exceeded)
  </action>
  <verify>
    <automated>cd /root/lobsec && python3 -c "
import sys
sys.path.insert(0, 'packages/scraper/src')
from ninja_scraper.api.main import app
from ninja_scraper.api.schemas import ScrapeRequest, CrawlRequest, CrawlResponse, JobStatus
from ninja_scraper.api.auth import verify_token
print('All API imports OK')
# Check routes exist
routes = [r.path for r in app.routes]
assert '/scrape' in routes, 'Missing /scrape route'
assert '/health' in routes, 'Missing /health route'
assert '/missions' in routes, 'Missing /missions route'
print(f'Routes OK: {routes}')
"</automated>
  </verify>
  <done>
    - FastAPI app has /scrape, /crawl, /crawl/{job_id}, /missions, /health endpoints
    - Bearer token auth protects all endpoints except /health
    - Background task execution tracks job state in memory
    - App binds to 127.0.0.1:18791
    - ScrapeRequest, CrawlRequest, CrawlResponse, JobStatus Pydantic models defined
  </done>
</task>

</tasks>

<verification>
1. All source files exist under packages/scraper/src/ninja_scraper/
2. pyproject.toml is valid with correct dependencies
3. Mission model validates correctly
4. FastAPI routes are registered
5. Auth dependency rejects missing/invalid tokens
</verification>

<success_criteria>
- Complete @lobsec/scraper package at packages/scraper/ with engine, API, stealth, and utils modules
- Mission YAML schema defined and validatable via Pydantic
- FastAPI app with 5 endpoints (/scrape, /crawl, /crawl/{job_id}, /missions, /health)
- Bearer token authentication on protected endpoints
- Patchright stealth browser launcher
- Retry with exponential backoff and jitter
- Structured JSON logging via structlog
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-ninja-scraper/07.1-01-SUMMARY.md`
</output>
