---
phase: 07-mvp-collection
task: UAT
total_tasks: 4 plans complete, UAT in progress
status: blocked
last_updated: 2026-03-11T20:14:48.936Z
---

<current_state>
Phase 7 code is complete and deployed. All 4 plans executed, plugin loads with 7 collectors + 1 tool. BUT: ALL 7 COLLECTORS FAIL AT RUNTIME. Zero data collected. This is a critical blocker — the entire phase deliverable (data collection) doesn't work.
</current_state>

<completed_work>

- Plan 07-01: Normalization pipeline foundation — area mapping (153 areas), orchestrator, gap detection, volume validation, pandera. WORKS.
- Plan 07-02: DLD sales, Ejari rentals, building permits collectors (CSV-based). CODE COMPLETE but fetch() blocked by Dubai Pulse WAF.
- Plan 07-03: ADREC Abu Dhabi, Bayut, PropertyFinder collectors (Playwright-based). CODE COMPLETE but "circuit breaker is open" at runtime.
- Plan 07-04: DEWA connections collector, registry wiring, CLI, deployment. DEPLOYED but "circuit breaker is open" at runtime.
- Deployment fix: SQLite WAL blocker resolved (UAE_RE_DATA_DIR path + systemd ReadWritePaths).
- UAT: 4/7 tests passed (plugin registration, DB structure, area seeding, TypeScript compilation, Python imports). 1 failed (collection), 1 skipped (Telegram tool).
</completed_work>

<remaining_work>

CRITICAL: Fix all 7 collectors so they actually return data.

Two distinct failure modes:
1. **Dubai Pulse WAF (3 collectors: dld-sales, ejari-rentals, building-permits)**: Plain `fetch()` to dubaipulse.gov.ae gets "Request Rejected" HTML. Need Playwright browser automation OR find the actual open data API endpoint (Dubai Pulse has a data API at data.bayanat.ae or similar).
2. **Circuit breaker (4 collectors: adrec-abu-dhabi, bayut-listings, propertyfinder-listings, dewa-connections)**: These use Playwright but fail with "Circuit breaker is open" before even attempting. Likely causes:
   - Playwright not installed in production (`npx playwright install chromium` never ran as lobsec user)
   - Circuit breaker in collector framework tripped from previous failures and not resetting
   - Missing browser dependencies on the server

After fixing collectors:
- Re-run UAT test 5 (collector execution)
- Test Telegram tool (UAT test 4)
- Verify normalization pipeline end-to-end with real data
</remaining_work>

<decisions_made>

- Area lookup: exact match only (no fuzzy until QUAL-04 Phase 12)
- Normalization upsert: DELETE+INSERT for idempotent re-runs
- Gap detection: 2x frequency threshold
- Volume validation: N=4 rolling baseline
- ADREC replaces DARI/UAE Pass (no auth needed)
- Anti-bot: random delays, no retry on block
- First page only for listing scrapers (MVP)
- Data dir: /opt/lobsec/data (fscrypt-encrypted, in systemd ReadWritePaths)
</decisions_made>

<blockers>

- BLOCKER: 7/7 collectors fail — zero data collected
- Dubai Pulse WAF blocks fetch() for 3 CSV collectors
- Circuit breaker blocks 4 Playwright collectors (likely Playwright not installed in prod)
- nftables egress firewall may also be blocking outbound connections to collector target domains
</blockers>

<context>
The user is frustrated — rightly so. We built a data collection system that can't collect data. The infrastructure works (DB, areas, normalization pipeline, plugin registration) but the actual core function — downloading data from external sources — was never tested until UAT. The agents wrote code against assumed URL patterns and download mechanisms without verifying they work against real websites.

Priority for next session: Don't plan, don't architect — just make ONE collector actually return data. Start with the circuit breaker investigation (likely quick fix: install Playwright browsers), then tackle the Dubai Pulse WAF issue. Check nftables too.

Key investigation order:
1. Check if Playwright chromium is installed: `sudo -u lobsec npx playwright install chromium`
2. Check circuit breaker code in collector framework (packages/uae-re/src/collectors/base.ts)
3. Check nftables rules: `nft list ruleset` — are collector target domains whitelisted?
4. For Dubai Pulse: research actual API endpoint (may be data.bayanat.ae or need API key)
5. Test one Playwright collector after fixes
6. Test one CSV collector after fixes
</context>

<next_action>
Start with: `sudo -u lobsec npx playwright install chromium` to install browser binaries, then check the circuit breaker logic in `packages/uae-re/src/collectors/base.ts`. Also check `nft list ruleset` for egress firewall rules that might block outbound HTTP to collector domains.
</next_action>
