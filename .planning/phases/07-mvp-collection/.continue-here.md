---
phase: 07-mvp-collection
task: ninja-scraper-phase-setup
status: complete
last_updated: 2026-03-12T15:42:00Z
---

<current_state>
Phase 7.1 (Ninja Scraper) COMPLETE. Python scraping engine deployed as lobsec-scraper.service.
7 YAML missions loaded, engine verified for HTTP download and browser scrape mission types.
TS CollectorRegistry refactored to thin HTTP client calling scraper API.
Phase 7 original collectors replaced by Ninja Scraper. Ready for Phase 8 (Tier B Collection).
</current_state>

<completed_work>
- Playwright chromium installed for lobsec user
- better-sqlite3 native addon rebuilt
- nftables verified (port 443 open)
- All 7 collector endpoints tested:
  - ADREC: accessible (dari.ae direct URL works, export buttons work)
  - Dubai Pulse (DLD, Ejari, Permits, DEWA): WAF blocked, needs OAuth API
  - Bayut: CAPTCHA, needs stealth browser
  - PropertyFinder: CloudFront 403
- ADREC collector rewritten (partially working — summary not detail data)
- Dubai Pulse API research complete (OAuth 2.0 at api.dubaipulse.gov.ae)
</completed_work>

<decision>
User wants to build "Ninja Scraper 2.0" — a Python scraping engine using:
- **Crawlee for Python** with PlaywrightCrawler for orchestration
- **Patchright** (undetected Playwright fork) for stealth browser
- Full Python rewrite of all 7 collectors (not TypeScript anymore)
- Integration: Collectors become Python, TS only for OpenClaw plugin orchestration

Design document provided by user is at conversation level — summarize key points:
- Mission-based architecture (YAML/JSON mission specs)
- Stealth-first: Patchright, proxy rotation, realistic behavior
- Extraction engine: CSS/XPath selectors, JSON output
- Fallback engine: detect failures, trigger alternative missions
- API layer: FastAPI with /scrape, /crawl, /missions endpoints (Firecrawl-inspired)
- Conservative defaults: max_concurrency=2, 2-6s random delays, exponential backoff
</decision>

<next_action>
Phase 7.1 complete. Next steps:
1. Phase 8: Tier B Collection (8 demographic/employment data sources with scheduling)
2. User still needs to register for Dubai Pulse API credentials (enables 4 of 7 Tier A sources)
3. Bayut/PropertyFinder CSS selectors need tuning to match actual DOM (Phase 8 or iterative)
</next_action>
